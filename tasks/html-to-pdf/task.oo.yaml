inputs_def:
  - handle: html_content
    description: "%html-content-to-convert2%"
    json_schema:
      type: string
      ui:widget: text
    nullable: false

  - handle: title
    description: "%document-title%"
    json_schema:
      type: string
    value:
    nullable: true

  - handle: font_size
    description: "%base-font-size2%"
    json_schema:
      type: number
      enum:
        - 10
        - 11
        - 12
        - 14
        - 16
      ui:options:
        labels:
          - 10pt (Small)
          - 11pt (Normal)
          - 12pt (Large)
          - 14pt (Extra Large)
          - 16pt (Huge)
    value: 11
    nullable: false

  - handle: output_path
    description: "%output-pdf-file-path-optional-uses-session-dir-if-not-set%"
    json_schema:
      type: string
      ui:widget: save
      ui:options:
        filters:
          - name: PDF
            extensions:
              - pdf
        excludeAll: true
    value:
    nullable: true

outputs_def:
  - handle: pdf_path
    description: "%generated-pdf-file-path2%"
    json_schema:
      type: string
    nullable: false

executor:
  name: python
  options:
    entry: __init__.py

title: "%html-to-pdf3%"
icon: ":carbon:document-pdf:"
description: "%convert-html-to-pdf-using-weasyprint-with-otf-font-support%"
